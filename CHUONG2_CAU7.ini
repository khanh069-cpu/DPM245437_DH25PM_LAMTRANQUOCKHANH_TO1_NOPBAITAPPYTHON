"""
Trong Python, cách cơ bản nhất để nhập dữ liệu từ bàn phím là sử dụng hàm input(), hàm này luôn trả về một chuỗi (string). Để nhập các kiểu dữ liệu khác như số nguyên hoặc số thực, bạn cần kết hợp input() với các hàm chuyển đổi kiểu dữ liệu tương ứng, 
ví dụ: int(input()) để nhập số nguyên và float(input()) để nhập số thực. 

"""